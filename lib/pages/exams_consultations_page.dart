import 'package:flutter/material.dart';

class ExamsConsultationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Exames e Consultas - Em desenvolvimento',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
