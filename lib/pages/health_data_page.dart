import 'package:flutter/material.dart';

class HealthDataPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Dados de Saúde - Em desenvolvimento',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
